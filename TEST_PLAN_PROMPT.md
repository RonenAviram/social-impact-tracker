# פרומפט לצ'אט הבא — תכנית בדיקות מקיפה

להדביק בצ'אט חדש עם אותו פרויקט (Social_Impact_Tracker):

---

## הקשר

אתה צוות QA שלם שמריץ בדיקות על מערכת **Social Impact Tracker** — כלי למדידת אימפקט של קמפיינים ברשתות חברתיות עבור **"קווים אדומים"**, שותפות ישראלית למניעת אלימות בזוגיות. המערכת חיה ב-GitHub Pages ומחוברת ל-Supabase backend.

### המערכת

**5 קבצי HTML + config.js**, מתארחת ב: `https://ronenaviram.github.io/social-impact-tracker/`

- **config.js** — מקור אמת: Supabase URL/key, campaign ID, 11 פלטפורמות, 8 ארגונים, session helpers
- **form.html** — לוגין + הזנת תוכן (משתמש רגיל)
- **dashboard.html** — דשבורד אגרגטיבי עם גרפים, מטרות, פילטרים, ארכיון קישורים
- **admin-pool.html** — ניהול מאגר אנשים וארגונים (CRUD, CSV import/export). **אין הגנת סיסמה** — גישה ישירה
- **admin-campaign.html** — יצירת קמפיין חדש (wizard: פרטים → משתתפים → יעדים → סיכום). **אין הגנת סיסמה** — גישה ישירה

### טכנולוגיה

- **Supabase** (PostgreSQL): פרויקט `siufqucjxcuhdfexzfkq`
- **Auth**: לא Supabase Auth — בחירת שם+ארגון מ-dropdown. לוגין דרך `get_campaign_pool` RPC שמחזיר את רשימת המשתתפים, ואז התאמה בצד הלקוח
- **Campaign ID**: `87ffac94-18ab-43e3-bedb-f7cc877973f8`
- **Admin password**: `nadav` (verified server-side via `verify_admin` RPC with SHA-256) — רק בדשבורד, לא בדפי admin
- **RLS**: פתוח לקריאה, כתיבה דרך RPC SECURITY DEFINER

### DB Schema

7 טבלאות: organizations, profiles, campaigns, campaign_participants, campaign_goals, content_entries, metrics_snapshots

10 RPC functions: login_user (unused), get_campaign_pool, get_all_profiles, verify_admin, add_content_entry, update_metrics, delete_content_entry, get_user_contents, get_dashboard_data, get_campaign

### Seed Data

**8 ארגונים:** ויצו, נעמת, דלת פתוחה, לדעת לבחור נכון, איגוד העובדים הסוציאליים, לתת פה, לא לאלימות, קווים אדומים

**10 פרופילים:**
1. מיכל פרינס — קווים אדומים (admin)
2. דנה כהן — ויצו
3. יעל לוי — נעמת
4. רותם שמש — דלת פתוחה
5. אורלי דוד — לדעת לבחור נכון
6. נועה בן דוד — איגוד העובדים הסוציאליים
7. שירה גולן — לתת פה
8. מאיה ברק — לא לאלימות
9. תמר רוזן — קווים אדומים
10. ליאת כץ — ויצו

**קמפיין:** "מניעת אלימות בזוגיות צעירה" (active, 2026-06-22 → 2026-06-28)
**יעדים (מטבלת campaign_goals):** חשיפות כוללות (2M), תכנים שהועלו (150), חשיפות ממוצעות לפוסט FB (1000), משתתפים פעילים (50)

### 11 פלטפורמות ו-metrics

- **פייסבוק**: פוסט/סרטון/רילס/סטורי/לייב → likes, comments, shares (engagement), reach (reach), video_views (only סרטון/רילס/לייב)
- **אינסטגרם**: פוסט/רילס/סטורי/קרוסלה → likes, comments, saves (engagement), reach (reach), views (only רילס/סטורי)
- **טיקטוק**: סרטון/לייב → views (reach), likes, comments, shares, saves (engagement)
- **X**: ציוץ/ציוץ מצוטט/תגובה/Thread → likes, retweets, replies (engagement), impressions (reach)
- **לינקדאין**: פוסט/מאמר/סרטון/מסמך → likes, comments, shares (engagement), impressions (reach)
- **ספוטיפיי**: פרק פודקאסט → listens (reach), comments (engagement), new_followers, avg_listen
- **יוטיוב**: סרטון/שורטס/לייב → views (reach), likes, comments, shares (engagement), new_subscribers
- **ניוזלטר**: ניוזלטר/עדכון → recipients (reach), opens, clicks (engagement)
- **עיתון מודפס**: כתבה/מודעה/טור דעה → circulation (reach), page
- **אתר אינטרנט**: כתבה/דף נחיתה/פוסט בבלוג → pageviews (reach), unique_visitors, avg_time
- **וואטסאפ**: סטטוס/הודעה בקבוצה/העברה → views (reach), forwards (engagement), groups_shared

### מבנה פאנל אדמין בדשבורד (אחרי ניקוי)

הפאנל מכיל:
- **כפתורים:** קמפיין חדש (→ admin-campaign.html), ניהול מאגר (→ admin-pool.html), ייצוא CSV
- **כרטיס סטטוס:** dropdown לשינוי סטטוס קמפיין (פעיל/מושהה/הסתיים) — מחובר ל-DB
- **אין** שדות עריכת יעדים או פרטי קמפיין בפאנל (הוסרו — ניהול דרך admin-campaign.html בלבד)

---

## דרישות מקדימות — בצע לפני שמתחילים

**נחה את המשתמש (רונן) צעד אחר צעד דרך הצעדים הבאים. אל תתחיל בדיקות עד שכולם עוברים:**

**P1. Chrome extension מחובר**
- ודא שיש לך גישה לכלי `mcp__claude-in-chrome__*`
- נסה `tabs_context_mcp` — אם עובד, ה-extension מחובר
- אם לא — בקש מרונן להתחבר לתוסף Claude in Chrome

**P2. Supabase מחובר**
- נווט דרך Chrome ל: `https://supabase.com/dashboard/project/siufqucjxcuhdfexzfkq`
- אם לא מחובר — בקש מרונן להתחבר לחשבון Supabase

**P3. DB נקי — אפס content entries**
- ב-Supabase SQL Editor, הרץ:
```sql
SELECT count(*) FROM content_entries WHERE campaign_id = '87ffac94-18ab-43e3-bedb-f7cc877973f8';
```
- אם התוצאה > 0, הרץ:
```sql
DELETE FROM content_entries WHERE campaign_id = '87ffac94-18ab-43e3-bedb-f7cc877973f8';
```
- זה קריטי — חלק A בודק empty state ויכשל אם יש נתונים

**P4. אישור מרונן**
- הצג לרונן: "דרישות מקדימות עברו. מוכן להריץ ~40 בדיקות. בדרך אתקן באגים חוסמים מיד, באגים קוסמטיים אתעד ואתקן בסוף. בסיום — push אחד ודיווח. להתחיל?"

---

## כללי עבודה

- **כלי עבודה:** השתמש ב-Claude in Chrome לנווט ולתפעל את האתר. Supabase SQL Editor דרך Chrome.
- **באג חוסם** (מונע המשך בדיקות) → תקן את הקוד מיד, commit, והמשך. **אל תעשה push** — צבור commits.
- **באג קוסמטי** (לא מונע המשך) → תעד והמשך. תקן בסוף.
- **Push** — push אחד מרוכז בסוף. רונן יריץ `git push` מה-CMD כי הסנדבוקס לא יכול לעשות push ישירות.
- **ניקוי** — בסוף הבדיקות, נקה את כל ה-test data מ-Supabase.

### חלק A — Empty state ודשבורד ריק (dashboard.html)

**חשוב: הרץ את חלק A לפני הזנת נתונים!**

**A1. באנר "אין נתונים"**
- נווט ל-dashboard.html
- ודא שמתחת לכרטיסי הסטטיסטיקה מופיע באנר "אין נתונים להצגה"
- ודא שכל הערכים בכרטיסים הם 0 או "—"

**A2. גרפים ריקים**
- ודא שכל 4 הגרפים מציגים "אין נתונים להצגה" (בלי צירים, בלי מספרים, בלי charts ריקים)
- 4 הגרפים: תכנים לפי פלטפורמה (donut), העלאות תוכן לאורך זמן (bar), אינטראקציות לפי סוג (bar), חשיפות לאורך זמן (line)

**A3. פילטר chips — קיימים גם ללא נתונים**
- ודא ש-11 chips של פלטפורמות מופיעים (+ "הכל")
- ודא ש-8 chips של ארגונים מופיעים (+ "הכל")

**A4. ארכיון קישורים ריק**
- גלול לארכיון → ודא שמציג "אין קישורים להצגה" ולשונית "הכל (0)"

### חלק B — בדיקות לוגין ו-session (form.html)

**B1. טעינת מאגר משתתפים**
- נווט ל-form.html
- ודא ש-dropdown שמות מכיל בדיוק 10 שמות (מ-Supabase, לא hardcoded)
- ודא שארגון ריק עד שבוחרים שם

**B2. מיפוי שם→ארגון**
- בחר "דנה כהן" → ודא שארגון מראה רק "ויצו"
- בחר "מיכל פרינס" → ודא שארגון מראה רק "קווים אדומים"
- בחר "ליאת כץ" → ודא שארגון מראה רק "ויצו" (שני אנשים מאותו ארגון)

**B3. כניסה מוצלחת**
- בחר שם + ארגון → לחץ כניסה
- ודא מעבר למסך אישי עם שם+ארגון+אותיות ראשוניות נכונות
- ודא שהמסך מראה "עדיין לא העלית תכנים"

**B4. Session persistence**
- אחרי כניסה, רענן את הדף (F5)
- ודא שהמשתמש עדיין מחובר (לא חוזר ללוגין)

**B5. החלפת משתמש**
- לחץ "החלף משתמש"
- ודא חזרה למסך לוגין
- ודא ש-localStorage נוקה

### חלק C — הזנת תוכן (form.html) — סימולציית קמפיין

**זה הליבה של הבדיקות.** הזן תוכן עבור 4 משתתפים שונים מ-4 ארגונים שונים כדי לדמות קמפיין אמיתי:

**C1. משתתפת 1 — דנה כהן (ויצו) — פייסבוק**
- התחבר כדנה כהן
- לחץ "הוסף תוכן" → בחר פייסבוק
- סוג: "פוסט" → הזן:
  - קישור: `https://facebook.com/test-post-1`
  - תאריך: 2026-06-23
  - likes: 150, comments: 23, shares: 45, reach: 5200
- שמור → ודא שהתוכן מופיע ברשימה האישית עם הנתונים הנכונים
- הוסף תוכן שני: פייסבוק → "סרטון":
  - קישור: `https://facebook.com/test-video-1`
  - תאריך: 2026-06-24
  - likes: 89, comments: 12, shares: 30, reach: 8400, video_views: 3200
  - ודא ש-video_views מופיע (כי זה סרטון)
- התנתק

**C2. משתתפת 2 — יעל לוי (נעמת) — אינסטגרם + טיקטוק**
- התחבר כיעל לוי
- הוסף תוכן: אינסטגרם → "רילס":
  - קישור: `https://instagram.com/reel/test-1`
  - תאריך: 2026-06-23
  - likes: 340, comments: 56, saves: 120, reach: 12000, views: 8500
  - ודא ש-views מופיע (כי רילס)
- הוסף תוכן: טיקטוק → "סרטון":
  - קישור: `https://tiktok.com/@test/video/1`
  - תאריך: 2026-06-24
  - views: 45000, likes: 3200, comments: 180, shares: 890, saves: 450
- התנתק

**C3. משתתפת 3 — שירה גולן (לתת פה) — ניוזלטר + אתר**
- התחבר כשירה גולן
- הוסף תוכן: ניוזלטר → "ניוזלטר":
  - קישור: `https://mailchimp.com/test-newsletter`
  - תאריך: 2026-06-22
  - recipients: 2500, opens: 890, clicks: 234
- הוסף תוכן: אתר אינטרנט → "כתבה":
  - קישור: `https://example.com/article-1`
  - תאריך: 2026-06-25
  - pageviews: 1800, unique_visitors: 1200, avg_time: 180
- התנתק

**C4. משתתפת 4 — מיכל פרינס (קווים אדומים, admin) — לינקדאין + יוטיוב**
- התחבר כמיכל פרינס
- הוסף תוכן: לינקדאין → "מאמר":
  - קישור: `https://linkedin.com/pulse/test-article`
  - תאריך: 2026-06-23
  - likes: 78, comments: 15, shares: 22, impressions: 4500
- הוסף תוכן: יוטיוב → "סרטון":
  - קישור: `https://youtube.com/watch?v=test123`
  - תאריך: 2026-06-24
  - views: 6700, likes: 320, comments: 45, shares: 88, new_subscribers: 12
- **אל תתנתק** (תישאר מחובר לבדיקות עדכון)

### חלק D — עדכון ומחיקת תוכן (form.html)

**D1. עדכון מטריקות**
- בתור מיכל פרינס, מצא את פוסט הלינקדאין ברשימה
- לחץ "עדכון נתונים"
- שנה: likes: 156, comments: 34, shares: 41, impressions: 9200
- שמור → ודא שהערכים המעודכנים מופיעים

**D2. מחיקת תוכן**
- הוסף תוכן חדש לצורך מחיקה: פייסבוק → "סטורי":
  - קישור: `https://facebook.com/story/delete-me`
  - תאריך: 2026-06-25
  - likes: 10, comments: 2, shares: 1, reach: 500
- מצא אותו ברשימה → לחץ "מחק"
- ודא הודעת אישור → אשר מחיקה
- ודא שנעלם מהרשימה
- התנתק

### חלק E — דשבורד עם נתונים (dashboard.html)

**E1. טעינת נתונים**
- נווט ל-dashboard.html
- ודא שבאנר "אין נתונים להצגה" **נעלם**
- ודא שכרטיסי סטטיסטיקה מראים ערכים > 0:
  - חשיפות כוללות (reach+views+impressions+recipients+pageviews)
  - אינטראקציות (likes+comments+shares+saves+opens+clicks+forwards)
  - תכנים (מספר הפריטים שהוזנו, פחות המחוק)
  - משתתפים פעילים (4 — דנה, יעל, שירה, מיכל)

**E2. פילטר פלטפורמה**
- לחץ על chip "פייסבוק" → ודא שרק תכני פייסבוק מוצגים
- לחץ על chip "אינסטגרם" בנוסף → ודא multi-select (פייסבוק + אינסטגרם)
- לחץ על "הכל" → ודא חזרה לתצוגה מלאה

**E3. פילטר ארגון**
- לחץ על ארגון ספציפי (ויצו) → ודא סינון לתכנים של דנה בלבד (ליאת לא העלתה תוכן)
- לחץ "הכל" → חזרה

**E4. יעדים (goals)**
- ודא שמוצגים 4 יעדים עם progress bars (מטבלת campaign_goals ב-DB)
- ודא שיש חישוב % ביחס ליעד

**E5. גרפים עם נתונים**
- ודא שגרף "תכנים לפי פלטפורמה" מראה donut chart עם פלטפורמות שיש בהן תוכן
- ודא שגרף "העלאות תוכן לאורך זמן" מראה עמודות לפי ימים
- ודא שגרף "חשיפות לאורך זמן" מראה קו עולה מצטבר
- ודא שגרף "אינטראקציות לפי סוג" מראה עמודות פירוט
- לחץ על פלטפורמה בפילטר → ודא שגרף אינטראקציות מתעדכן לפי הפלטפורמה

**E6. ארכיון קישורים**
- גלול לארכיון קישורים
- ודא שכל הקישורים שהוזנו מופיעים (7 תכנים פחות 1 שנמחק = 6 קישורים)
- ודא פילטר לפי פלטפורמה עובד
- לחץ "העתק" על קישור → ודא toast "הקישור הועתק!"

**E7. נתונים גולמיים (מצב אדמין)**
- סמן checkbox "מנהל" → הזן סיסמה "nadav"
- ודא שפאנל אדמין מופיע עם: dropdown סטטוס, כפתורי ניווט (קמפיין חדש, ניהול מאגר), ייצוא CSV
- ודא ש**אין** שדות עריכת יעדים או פרטי קמפיין בפאנל
- ודא שטבלת נתונים גולמיים מראה את כל הרשומות

**E8. שינוי סטטוס קמפיין**
- שנה סטטוס ל"מושהה" → ודא באנר צהוב "הקמפיין מושהה..."
- עבור ל-form.html → נסה להוסיף תוכן → ודא שנחסם עם הודעה
- חזור לדשבורד → שנה חזרה ל"פעיל"
- ודא שהבאנר נעלם

**E9. ייצוא CSV**
- לחץ "ייצוא CSV" → ודא שמוריד קובץ CSV תקין עם הנתונים

### חלק F — אדמין: ניהול מאגר (admin-pool.html)

**F1. כניסה ישירה (בלי סיסמה)**
- נווט ל-admin-pool.html
- ודא שהדף נטען ישירות בלי prompt לסיסמה

**F2. צפייה במאגר**
- ודא ש-10 אנשים מוצגים
- ודא ש-8 ארגונים מוצגים עם ספירת אנשים ליד כל אחד

**F3. הוספת ארגון חדש**
- הוסף ארגון: "ארגון בדיקה"
- ודא שמופיע ברשימת ארגונים

**F4. הוספת אדם חדש**
- הוסף: שם "בודק בדיקות", ארגון "ארגון בדיקה"
- ודא שמופיע ברשימה

**F5. חיפוש**
- הקלד "דנה" בשדה חיפוש → ודא שרק דנה כהן מוצגת
- נקה חיפוש → ודא שכולם חזרו

**F6. מחיקת אדם (soft delete)**
- מחק את "בודק בדיקות"
- ודא שנעלם מהתצוגה
- (הוא עדיין ב-DB עם is_active=false)

**F7. ניסיון מחיקת ארגון עם אנשים**
- נסה למחוק ארגון שיש בו אנשים פעילים
- ודא שמופיעה הודעת שגיאה (לא ניתן למחוק)

**F8. מחיקת ארגון ריק**
- מחק את "ארגון בדיקה" (אין בו אנשים פעילים אחרי F6)
- ודא שנמחק בהצלחה

**F9. ייצוא CSV מאגר**
- לחץ "ייצוא CSV" → ודא שמוריד קובץ CSV עם שם,ארגון,קמפיינים

### חלק G — אדמין: יצירת קמפיין (admin-campaign.html)

**G1. כניסה ישירה (בלי סיסמה)**
- נווט ל-admin-campaign.html
- ודא שהדף נטען ישירות בלי prompt לסיסמה

**G2. Wizard שלב 1 — פרטי קמפיין**
- שם: "קמפיין בדיקות"
- תאריכים: 2026-08-01 → 2026-08-07
- המשך לשלב הבא

**G3. Wizard שלב 2 — משתתפים**
- לחץ "ייבוא מהמאגר" → ודא שכל האנשים הפעילים מיובאים
- ודא שמוצג מספר משתתפים נכון
- נסה להוסיף אדם ידנית (שם + ארגון)

**G4. Wizard שלב 3 — יעדים**
- הוסף יעד: "חשיפות כוללות", ערך: 1000000
- הוסף יעד: "תכנים שהועלו", ערך: 50
- ודא שכפתור "הוסף יעד" נעלם אחרי 3 (MAX_GOALS)
- המשך

**G5. Wizard שלב 4 — סיכום והשקה**
- ודא שהסיכום מציג את כל הפרטים נכון (שם, תקופה, ארגונים, משתתפים, יעדים)
- **אל תלחץ "השק קמפיין"** — רק ודא שהסיכום תקין
- (לא משיקים כדי לא לזהם את ה-DB)

### חלק H — בדיקות edge cases

**H1. קישור ריק**
- התחבר כמשתמש → נסה להוסיף תוכן בלי קישור
- ודא שעובד (קישור הוא אופציונלי)

**H2. XSS בקישור**
- נסה להזין קישור: `javascript:alert(1)`
- ודא שנחסם (sanitizeLink)

**H3. ערכי metrics אפסיים**
- הזן תוכן עם כל ה-metrics = 0
- ודא ששומר בהצלחה

**H4. Session בדפים שונים**
- התחבר ב-form.html
- עבור ל-dashboard.html
- ודא שה-session נשמר (localStorage)

**H5. Campaign ended**
- בדשבורד אדמין: שנה סטטוס ל"הסתיים"
- עבור ל-form.html → ודא שנחסם עם הודעה "הקמפיין הסתיים"
- ודא badge "הסתיים" מופיע ליד שם הקמפיין
- חזור לדשבורד → שנה חזרה ל"פעיל"

### חלק I — ניקוי

**I1. מחיקת test data**
- פתח Supabase SQL Editor (`https://supabase.com/dashboard/project/siufqucjxcuhdfexzfkq/sql`)
- הרץ:
```sql
-- Delete test content entries (cascades to metrics_snapshots)
DELETE FROM content_entries WHERE campaign_id = '87ffac94-18ab-43e3-bedb-f7cc877973f8';

-- Delete test campaign if created in part G
DELETE FROM campaigns WHERE name = 'קמפיין בדיקות';

-- Delete test org/profile if still in DB
DELETE FROM profiles WHERE name = 'בודק בדיקות';
DELETE FROM organizations WHERE name = 'ארגון בדיקה';
```
- ודא שה-DB חזר למצב נקי (seed data בלבד, אפס content_entries)

---

## פורמט דיווח

בסוף הבדיקות, כתוב סיכום:
- **עבר ✅ / נכשל ❌** לכל בדיקה
- באגים שנמצאו + האם תוקנו
- המלצות לשיפור
- עדכן את כל קבצי הזיכרון (MEMORY.md וכו') עם תוצאות הבדיקות

---

**התחל מדרישות מקדימות (P1-P4). רק אחרי שכולן עוברות — התחל את תכנית הבדיקות.**
